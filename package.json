{
  "dependencies": {
    "axios": "^1.2.2",
    "cookie-parser": "^1.4.6",
    "crypto": "^1.0.1",
    "disconnect": "^1.2.2",
    "dotenv": "^16.0.3",
    "esbuild": "0.17.2",
    "express": "^4.18.2",
    "express-fileupload": "^1.4.0",
    "localtunnel": "^2.0.2",
    "pug": "^3.0.2"
  },
  "scripts": {
    "build": "esbuild ./backend/server.js --bundle --outfile=backend.js --platform=node",
    "watch": "esbuild ./backend/server.js --bundle --outdir=frontend --platform=node --watch"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
